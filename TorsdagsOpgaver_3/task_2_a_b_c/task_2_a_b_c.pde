boolean happy = true;
int twoNumbers; //Her erklæerer jeg twoNumbers til at være en datatypen int.


boolean iAmHappy() {
  return happy = random(1)<0.5;
  //return happy = true;
}

int math (int a, int b) {
  return a + b; // her regnes de to int variabler (a og b) givet længere oppe
}
String textGettingBigger (String message) {
  return message.toUpperCase(); //retunere message beskedet i store bogstaver.
}
